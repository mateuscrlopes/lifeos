O modelo Vosk nao e versionado no Git por causa do tamanho.

Antes de compilar localmente, execute um destes scripts na raiz do projeto:

- android-gumate/scripts/baixar-modelo-vosk.ps1
- android-gumate/scripts/baixar-modelo-vosk.sh

O GitHub Actions tambem baixa o modelo automaticamente.
